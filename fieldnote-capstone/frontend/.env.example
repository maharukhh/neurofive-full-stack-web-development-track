# Copy to .env.local for local dev (optional — defaults to localhost:4001).
# In production (Vercel/Netlify), set VITE_API_BASE as a real environment
# variable pointing at your deployed backend.
VITE_API_BASE=http://localhost:4001
