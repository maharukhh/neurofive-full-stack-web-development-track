# In production (Render/Railway/Fly.io), set these as real environment
# variables in the service's dashboard.
PORT=4001
JWT_SECRET=replace-with-a-long-random-string
ALLOWED_ORIGIN=https://your-frontend.vercel.app
DB_PATH=./fieldnote.db
